[workspace]
resolver = "2"
members = [
    "crates/orbit-prefilter",
    "crates/tail-encoding",
    "crates/wire-frame",
    "api-server",
]
exclude = [
    "near-contract",    # Has its own build config (NEAR/WASM target)
    "keystore-worker",  # Has its own build config (WASI target)
]
